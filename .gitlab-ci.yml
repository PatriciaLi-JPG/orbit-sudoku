stages:
  - deploy

pages:
  stage: deploy
  script:
    - echo "Publishing Orbit Sudoku from prebuilt static files"
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
